{% extends '::base.html.twig' %}

{% block right_content -%}
    <h1>OrganisationunitStructure creation</h1>

    <form action="{{ path('organisationunitstructure_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('organisationunitstructure') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
