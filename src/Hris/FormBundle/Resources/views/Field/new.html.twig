{% extends '::base.html.twig' %}

{% block right_content -%}
    <h1>Field creation</h1>

    <form action="{{ path('field_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('field') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
