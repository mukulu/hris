{% extends '::base.html.twig' %}

{% block right_content -%}
    <h1>ArithmeticFilter creation</h1>

    <form action="{{ path('arithmeticfilter_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('arithmeticfilter') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
