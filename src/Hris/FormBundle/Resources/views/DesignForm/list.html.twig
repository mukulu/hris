{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}"/>
    <link rel="stylesheet" href="{{ asset('css/bootstrap.icon-large.min.css') }}"/>
    <link rel="stylesheet" href="{{ asset('commons/css/style.css') }}"/>
{% endblock %}

<div class="modal" id="showPopupModal">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
                <h4 class="modal-title"><i class="icon-circle-remove icon-largest" title="Delete"></i>Deleting</h4>
            </div>
            <div class="modal-body">

                <select id="content" name="content" size="10"
                        style="width: 100%; height: 90%; font-family: 'Courier New',Courier,mono; font-size: 12px;"
                        dir="ltr" wrap="soft">
                    {% for entity in entities %}
                        <option value="{{ entity.name }}">{{ entity.caption }}</option>
                    {% endfor %}
                </select>
            </div>
            <div class="modal-footer">
                <form action="" method="post">
                    <input type="hidden" class="btn btn-danger" name="_method" value="DELETE"/>

                    <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-danger">Delete</button>
                </form>

            </div>
        </div>
        <!-- /.modal-content -->
    </div>
    <!-- /.modal-dialog -->
</div><!-- /.modal -->
<!--<input type="button" value="Insert Field" class="btn"><input type="button" value="Cancel" class="btn">