{% extends '::base.html.twig' %}

{% block right_content -%}
    <h1>ResourceTable creation</h1>

    <form action="{{ path('resourcetable_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('resourcetable') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
