{% extends '::base.html.twig' %}

{% block right_content -%}
    <h1>Indicator list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Value</th>
                <th>Year</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('indicator_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.value }}</td>
                <td>{{ entity.year }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('indicator_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('indicator_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('indicator_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
